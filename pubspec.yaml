name: jumping_dot
description: We are aiming to provide an animated jumping dot indicators to Flutter developers.
version: 0.0.7
homepage: https://github.com/rezam92/jumping_dot

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
